form:
  validation: loose
  fields:
    section:
        type: section
        title: Add Article

    title:
      type: text
      label: NIGHT_RULES.ADMIN.ARTICLE_TITLE
      help: NIGHT_RULES.ADMIN.ARTICLE_TITLE_HELP
      validate:
        required: true

    folder:
      type: hidden
      default: '@slugify-title'

    route:
      type: hidden
      default: /blog

    name:
      type: hidden
      default: 'article'

    blueprint:
      type: blueprint
