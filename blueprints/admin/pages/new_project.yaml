form:
  validation: loose
  fields:
    section:
        type: section
        title: Add Project

    title:
      type: text
      label: NIGHT_RULES.ADMIN.PROJECT_TITLE
      help: NIGHT_RULES.ADMIN.PROJECT_TITLE_HELP
      validate:
        required: true

    folder:
      type: hidden
      default: '@slugify-title'

    route:
      type: hidden
      default: /home

    name:
      type: hidden
      default: 'project'

    blueprint:
      type: blueprint
